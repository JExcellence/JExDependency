# Flight Perk Configuration
id: fly
displayNameKey: perk_names.fly
descriptionKey: perk_descriptions.fly
category: movement
iconMaterial: ELYTRA
enabled: true

type: TOGGLEABLE
cooldownSeconds: 900
durationSeconds: 120

effect:
  type: FLIGHT
  allowInCombat: false

requirements:
  - type: RANK
    rankId: mage_4
  - type: CURRENCY
    currency: default
    amount: 10000
  - type: PERMISSION
    permission: rdq.perk.fly
