/**
 * Machine type registry — in-memory catalogue of placeable machines
 * loaded from YAML at startup. The database {@code Machine} entity
 * records per-placement state; the registry describes the blueprint.
 */
package de.jexcellence.quests.machine;
