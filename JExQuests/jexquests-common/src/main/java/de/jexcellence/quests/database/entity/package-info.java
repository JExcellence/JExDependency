/**
 * JExQuests JPA entities. All extend {@code LongIdEntity}; reward and
 * requirement configurations are stored as JSON blobs and parsed by the
 * service layer into sealed-interface domain types.
 */
package de.jexcellence.quests.database.entity;
