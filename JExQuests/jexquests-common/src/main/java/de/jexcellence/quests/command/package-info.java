/**
 * Handlers for JExQuests commands. Registered via
 * {@code CommandFactory.registerTree} with YAML-defined argument
 * schemas under {@code resources/commands/**}.
 */
package de.jexcellence.quests.command;
