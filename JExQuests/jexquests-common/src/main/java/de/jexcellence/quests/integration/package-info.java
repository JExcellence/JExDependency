/**
 * Optional integrations with other JExcellence plugins. Each wires
 * itself at startup only when the target plugin is detected on the
 * {@code ServicesManager}.
 */
package de.jexcellence.quests.integration;
