/**
 * InventoryFramework views for JExQuests. Registered on the
 * {@code ViewFrame} in {@link de.jexcellence.quests.JExQuests} during
 * enable. Non-paginated views back-navigate via
 * {@code super(PreviousView.class)}; paginated views drop through
 * the {@code <p>} token in their layout.
 */
package de.jexcellence.quests.view;
