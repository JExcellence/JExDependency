/**
 * Asynchronous Bukkit events fired by JExQuests. Subscribers see only
 * {@code api}-module snapshot records — no entity references leak across
 * the module boundary.
 */
package de.jexcellence.quests.api.event;
