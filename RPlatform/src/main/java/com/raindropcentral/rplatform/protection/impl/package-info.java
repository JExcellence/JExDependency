/**
 * Reflection-backed implementations of {@link com.raindropcentral.rplatform.protection.RProtectionBridge}.
 *
 * <p>Each implementation targets a single plugin and resolves API calls at
 * runtime to avoid introducing hard dependencies into {@code RPlatform}.</p>
 */
package com.raindropcentral.rplatform.protection.impl;
