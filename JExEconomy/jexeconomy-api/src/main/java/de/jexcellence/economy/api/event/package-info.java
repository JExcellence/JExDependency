/**
 * Bukkit events fired by the JExEconomy economy system.
 *
 * <p>All events use API-safe snapshot types ({@link de.jexcellence.economy.api.CurrencySnapshot},
 * player UUIDs) rather than internal entity classes, so third-party plugins have no
 * transitive dependency on JEHibernate or Jakarta Persistence.
 *
 * @author JExcellence
 * @since 3.0.0
 */
package de.jexcellence.economy.api.event;
