/**
 * Concrete services registered on the Bukkit {@code ServicesManager} for
 * third-party resolution. Each service wraps one or more repositories and
 * routes all failures through the platform logger.
 */
package de.jexcellence.core.service;
